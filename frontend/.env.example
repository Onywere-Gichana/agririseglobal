# URL of the deployed backend API, without a trailing slash.
VITE_API_URL=https://api.example.com

# Exact Cloudflare Pages project name from the Cloudflare dashboard.
CF_PAGES_PROJECT_NAME=agririseglobal
